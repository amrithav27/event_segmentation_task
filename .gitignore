docs/EXPERIMENTER.md

# config.toml must ship: it turns on the static file serving the videos need.
.streamlit/*
!.streamlit/config.toml
uv.lock
# Collected data stays local to each participant's machine.
responses/*/
!responses/.gitkeep

__pycache__/
*.pyc
.venv/
