[project]
name = "event-segmentation"
version = "1.0.0"
description = "Ego4D event-boundary segmentation experiment (coarse and fine)"
requires-python = ">=3.10"
dependencies = ["streamlit>=1.32"]

[tool.uv]
package = false
